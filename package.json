{
  "name": "levinson-layout-renderer",
  "version": "1.0.0",
  "description": "takes layout comments in a specific format and turns them into an interactive webapp",
  "source": "src/index.html",
  "context": "browser",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --no-optimize --no-cache --public-url /levinson-layout-renderer/",
    "deploy": "push-dir --dir=dist --branch=gh-pages --verbose"
  },
  "author": "Dan Wenger",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "parcel": "^2.5.0",
    "push-dir": "^0.4.1"
  }
}
