{
  "name": "levinson-layout-renderer",
  "version": "1.0.0",
  "description": "takes layout comments in a specific format and turns them into an interactive webapp",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --public-url /",
    "deploy": "gh-pages -d dist"
  },
  "author": "Dan Wenger",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "parcel": "^2.5.0",
    "push-dir": "^0.4.1"
  }
}
